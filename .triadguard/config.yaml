architect_rules:
  max_complexity: 10          # Maximum cyclomatic complexity per method
  strict_mode: true           # Automatically close the PR on critical findings
  ignore_security_rules: []   # Add CVE numbers here if they are false-positives
  excluded_paths:
    - "**/*.generated.cs"
    - "**/*.min.js"
