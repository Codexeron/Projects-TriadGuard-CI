rules:
  strict_mode: false          # true ise warnings bile PR'ı bloklar
  max_warnings: 10            # Uyarı sayısı bu eşiği geçerse PR reddedilir
  excluded_paths:
    - "**/*.generated.cs"
    - "**/*.min.js"
    - "**/migrations/*.py"
